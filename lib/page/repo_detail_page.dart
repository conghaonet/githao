import 'package:flutter/material.dart';

class RepoDetailPage extends StatelessWidget {
  const RepoDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      ),
      body: Container(

      ),
    );
  }
}
